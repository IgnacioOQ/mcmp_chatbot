# Python
__pycache__/
*.py[cod]
*$py.class
venv/
.env

# Project specific
data/
*.log
mcmp_chatbot.log
debug_env.py
scripts/test_*.py
scripts/verify_*.py
.DS_Store
.streamlit/secrets.toml

# Claude Code settings
.claude/settings.json
.claude/settings.local.json

# Firebase build
firebase/frontend/.env.local
firebase/frontend/node_modules/
firebase/frontend/.next/
firebase/.firebaserc
# Untracked: the lockfile has platform-specific optional deps (@emnapi/* sharp
# wasm) that differ macOS vs the Linux buildpack, breaking `npm ci`. Without it,
# App Hosting's buildpack runs `npm install` and resolves Linux deps correctly.
firebase/frontend/package-lock.json
